# repo-review-78-prototype-first-frontend-reconstruction

Prototype-first frontend reconstruction for the shared operational shell and Cashier routes, based on docs/repo_review_78.md.

## Repair status

The change is **not implementation-complete**. The repaired design/spec/evidence contract now contains the full route/state/role/viewport matrix, numeric shell ownership/formula, wrapper allowlist and actions, auxiliary-region order, 712px Find Customer HTML authority, artifact taxonomy, measurable search/System Online/Admin handoff assertions, Sync Queue DOM order, second-pass and final gates, and a reconciled historical/approved-deviation/current-blocker status table.

The current implementation and certification tasks remain unchecked. Historical validation counts are retained as historical records; the authoritative strict validation command/result is recorded in `evidence.md`.
