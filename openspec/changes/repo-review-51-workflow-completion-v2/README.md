# Repo review 51 workflow completion v2

This change is an exhaustive v2 of `docs/repo_review_51.md`.

It enumerates every identified frontend subgap line-by-line and converts each one into an implementation-ready OpenSpec workflow, without changing backend money or policy authority.
